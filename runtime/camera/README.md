# camera

Hides **where the view is and what drives it**. `fly_camera` is the free-flight
debug camera (and the transform every other driver writes into);
`showcase_camera` is the scripted cinematic flythrough used for trailers,
regression frames and benchmarks.
