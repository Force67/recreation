# demo

Hides **the non-gameplay presentation modes**. `demo_scenes` are the standalone
scenes `--demo` boots without game data (materials, water, actors); `trailer` is
the cinematic overlay (letterbox, title cards, render-mode badge) that the
showcase flythrough and the Helgen intro share.
