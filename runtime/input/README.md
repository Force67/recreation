# input

Hides **recreation's action set**. rx owns no gameplay verbs, so the game
declares which actions and axes exist and registers their INI names and default
bindings with an rx `InputMap` at startup.
