# interaction

Hides **acting on things in the world**. `interaction_system` finds what the
crosshair is over, decides whether it can be activated and supplies the prompt;
`item_bridge` moves items between world references and inventories and persists
what the player is carrying.
